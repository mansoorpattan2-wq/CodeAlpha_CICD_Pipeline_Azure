# Screenshot Checklist

Save each screenshot in this folder using the exact filenames below —
the main README already links to them.

| # | Filename | What to show |
|---|---|---|
| 00 | `00-prerequisites.png` | Terminal output of `python --version`, `git --version`, `docker --version`, `az --version` |
| 01 | `01-local-app.png` | Browser at `localhost:8000` showing the running Flask app |
| 02 | `02-docker-build.png` | Terminal showing `docker build` completing with "Successfully built/tagged" |
| 03 | `03-pipeline-success.png` | Azure DevOps pipeline run — both Build and Deploy stages green |
| 04 | `04-acr-repository.png` | Azure Portal → your ACR → Repositories, showing the pushed image and tags |
| 05 | `05-live-app.png` | Browser showing the live app at `https://<yourapp>.azurewebsites.net` |
| 06 | `06-deployment-center.png` | Azure Portal → App Service → Deployment Center, showing latest successful deployment |

Tip: Use `Win + Shift + S` for quick Windows screenshots, or Snipping Tool.
